namespace PolygonDraw
{
    /// <summary>
    /// Different ways a point can be contained by a polygon.
    /// </summary>
    enum ContainmentType
    {
        INSIDE,
        OUTSIDE,
        BOUNDARY,
    }
}